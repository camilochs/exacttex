\documentclass{article}
\begin{document}
\table(tab:main) {
  placement = ht
  caption = {A table}
  body = {
    \begin{tabular}{c} a \end{tabular}
  }
}
Table~@ref(tab:main) shows the layout.
\end{document}
