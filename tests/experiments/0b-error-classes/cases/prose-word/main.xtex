\documentclass{article}
\begin{document}
\table(tab:main) {
  placement = ht
  caption = {A table}
  body = {
    \begin{tabular}{c} a \end{tabular}
  }
}
Figure~@ref(tab:main) shows the layout.
\end{document}
