\table(t) {
  caption = {Latency by backend}
  body = { \begin{tabular}{@{}lcc@{}} a & b & c \\ \end{tabular} }
  trailing = {
    \vspace{2pt}
    {\scriptsize $^*$Values are medians over five runs.}
  }
}
